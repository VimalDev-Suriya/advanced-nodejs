# Buffers

Buffers are build in Object (global) used to handle the chunk of `Binary data`. The Buffer will be stored in Outside the V8 JS engine, so they are very performant oriented for have operations file File I/O and HTTP connections.

## Features:

1. Fixed size - Once initialized, we cannot change the size of it
2. Only takes Binary data
3. Highly performant
