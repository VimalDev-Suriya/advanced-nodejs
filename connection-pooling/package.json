{
  "name": "connection-pooling",
  "version": "1.0.0",
  "description": "A technique to cache the pre-established connections, so the successive request will re-use the cached request, instead of creating. establishing and destroying a TCP connection.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "dependencies": {
    "axios": "^1.18.0"
  }
}
